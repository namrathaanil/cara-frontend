import React from 'react';
import { Box, Typography } from '@mui/material';

const ConsultationReport: React.FC = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Report View Coming Soon</Typography>
    </Box>
  );
};

export default ConsultationReport;